# Copy this file to `.env.local` and fill in your real key.
# .env.local is gitignored — never commit your real OPENAI_API_KEY.

# OpenAI Realtime API key. Used SERVER-SIDE ONLY by /api/realtime-session.
# https://platform.openai.com/api-keys
OPENAI_API_KEY=sk-...replace-me...
